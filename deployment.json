{
  "_comment": "How the hosted copy of this dashboard is protected. See src/deployment.js.",
  "_options": "google = the app checks Google sign-in itself (needs 4 environment variables). password = the app asks for one shared password, using the hash in secrets.json. gateway = something in front of the app already checks (Vercel's own protection).",
  "accessMode": "password"
}
